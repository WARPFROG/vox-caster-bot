version: "2"

formatters:
  enable:
    - gofmt

linters:
  settings:
    errcheck:
      exclude-functions:
        - (io.ReadCloser).Close
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

issues:
  max-same-issues: 0
